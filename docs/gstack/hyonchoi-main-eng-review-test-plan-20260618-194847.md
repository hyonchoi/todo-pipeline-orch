# Test Plan
Generated by /plan-eng-review on 2026-06-18
Branch: main
Repo: todo-pipeline-orchestrator

## Affected Areas
- `hermes_pipeline/counter.py` (new) — recover_counter function
- `hermes_pipeline/cli.py` — recover-counter subcommand
- `hermes_pipeline/logging_setup.py` — verbose/debug level handling
- `hermes_pipeline/circuit.py` — new debug logging
- `hermes_pipeline/kanban.py` — new debug logging

## Key Interactions to Verify
- `pipeline-watch tick --debug <project>` surfaces raw agent payloads and circuit breaker internals
- `pipeline-watch tick --verbose <project>` surfaces lock state and selection summaries
- `pipeline-watch tick <project>` (no flag) behaves the same as before — no regression in log output
- `pipeline-watch recover-counter <project>` initializes or updates counter from TODOS.md
- `pipeline-watch recover-counter <project>` does not decrease an existing counter

## Edge Cases
- recover-counter with TODOS.md missing — should error clearly
- recover-counter with TODOS.md but no TODO entries — should write 0
- recover-counter with counter at 8 and TODOS.md max at 4 — should keep 8
- --verbose flag on non-tick subcommand — should work (global flag)
- --debug flag with existing INFO-level log output — DEBUG calls shouldn't appear without --debug

## Critical Paths
- New `vlog = logging.getLogger('pipeline.verbose')` pattern works across modules (cli.py, tick.py)
- circuit.py debug logging doesn't break import (module previously had no logging)
- All 281+ existing tests pass unchanged (no regression from log level changes)
