# Test Plan
Generated by /plan-eng-review on 2026-07-13
Branch: debug/todos-manager
Repo: todo-pipeline-orchestrator

## Affected Pages/Routes
- `skills/todos-manager/SKILL.md` — new `--list` workflow section added

## Key Interactions to Verify
- `todos-manager --list` shows formatted markdown table of active TODOs from TODOS.md
- `todos-manager --list --all` shows active + archived entries in two labeled tables
- `todos-manager --list` on project without TODOS.md shows error message
- `todos-manager --list` on empty TODOS.md shows "No active TODOs found."

## Edge Cases
- TODOS.md entries with missing summary (no ` — ` separator) → shows `[no summary]`
- TODOS.md entries with missing fields → shows `[not set]`
- TODOS-archive.md does not exist → skip archived section silently
- Titles containing `**` markup → strip markup correctly

## Critical Paths
- `todos-manager --list` → reads TODOS.md → regex parse → displays table → no files modified
