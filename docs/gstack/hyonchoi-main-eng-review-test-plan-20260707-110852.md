# Test Plan — TODO-7 phase_5_review
Generated by /autoplan (eng phase) on 2026-07-07
Branch: main
Repo: todo-pipeline-orchestrator

## Affected Files/Modules
- `configs/phases.yaml` — new `phase_5_review` entry between phase_4_development and phase_6_1_cso
- `hermes_pipeline/phases.py` — `load_phases()`, `_render_phase_prompt()`, `_invoke_hermes()` (no code change, but behavior depends on them)
- `tests/test_phases.py` — phase ordering + gate assertions

## Key Interactions to Verify
- `load_phases()` parses the new entry with all fields (phase_key, name, prompt, tools, turns, timeout)
- Phase sequence order after insertion: phase_2 → phase_3 → phase_4 → phase_5_review → phase_6_1_cso → phase_7 → phase_8 → phase_9
- `_render_phase_prompt()` substitutes `{todo_id}` in the diff-log path, findings path, and commit message
- `test_real_phases_yaml_ends_with_blocked_gate` still passes (phase_9 remains terminal gate)

## Edge Cases
- No-diff scenario: phase_4 produced no code changes (docs-only TODO) — review has nothing to review
- Non-main base branch: `git diff main` produces wrong baseline on integration branches
- Timeout during review: `hermes_agent_call` SIGKILLs, returns `timed_out=True` → `_invoke_hermes` raises RuntimeError → pipeline STOPS (contradicts "phase still completes")
- Multi-commit fixes: `/review` applies fixes across multiple commits; "revert the fix commit" (singular) leaves partial state
- `docs/pipeline/` directory missing: `git diff main > docs/pipeline/...log` redirect fails

## Critical Paths (must work)
1. YAML parse test — new phase loads with correct fields and position
2. Phase ordering test — assert the exact sequence after insertion
3. Autonomous-mode dry run — verify `/review` does NOT hang on confirmation inside `hermes chat -q` subprocess (SESSION_KIND/SPAWNED_SESSION detection)
4. Rollback path test — fixture branch with a deliberate test-breaking bug; verify graceful failure outcome

## LLM/Prompt Eval Note
The phase prompt is an LLM instruction blob. No deterministic unit test can verify "the LLM applied fixes correctly." A live dogfood run on a feature branch is the only real validation. Recommend running the phase manually on a recent feature branch before merging to main.
