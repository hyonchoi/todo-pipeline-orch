{
  "name": "hermes-pipeline",
  "version": "0.7.2",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "changeset": "changeset",
    "release:version": "changeset version && npm install --package-lock-only --ignore-scripts && python3 scripts/sync_release_version.py",
    "release:check": "python3 scripts/sync_release_version.py --check"
  },
  "devDependencies": {
    "@changesets/cli": "2.31.1"
  }
}
